<html lang="en">
  <head>
    <title>{{title}}</title>
    <style>
      body{
        margin: 0;
        padding: 0;
        background: #ccc;
      }
      a {
        display: block;
        margin-left: 30px;
        line-height: 30px;
      }
    </style>
  </head>
  <body>
  {{#each files}}
    <a href="{{../root}}/{{this}}">{{this}}</a>
  {{/each}}
  </body>
</html>